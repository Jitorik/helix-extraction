-- gamemodes/helix/schema/sh_schema.lua

Schema.name = "Helix RP Military"
Schema.description = "Built-in military RP schema for Helix framework."
Schema.author = "Custom"
Schema.version = "1.0"

-- Пустые функции, чтобы не было ошибок вызова
function Schema:Initialize()
end

function Schema:PlayerInitialSpawn(client)
end

function Schema:PlayerSpawn(client)
end